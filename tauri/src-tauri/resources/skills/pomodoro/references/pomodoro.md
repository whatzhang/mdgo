# 番茄钟模式细节

## 默认模式与时长

- 专注模式：默认 25 分钟
- 短休息模式：默认 5 分钟
- 长休息模式：默认 15 分钟
- 用户可根据需要自定义各模式时长

## 自动衔接规则

番茄钟可自动衔接模式，用户无需手动切换：

- **自动开始休息**：完成第 1、2、3 个专注后 → 进入短休息（默认 5 分钟）；完成第 4 个专注（streak % 4 === 0）→ 进入长休息（默认 15 分钟）；随后循环往复
- **自动开始专注**：完成休息模式后，自动进入专注模式
