{
  "name": "mdgo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "clean:target": "cd src-tauri ; cargo clean",
    "build:target": "npm run build ; cd src-tauri ; cargo build",
    "build:release": "npm run build ; cd src-tauri ; cargo build --release"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6",
    "vite-plugin-static-copy": "^4.1.1"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-fs": "^2",
    "@tauri-apps/plugin-store": "^2"
  }
}
